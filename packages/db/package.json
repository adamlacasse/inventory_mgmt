{
  "name": "@inventory/db",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "db:migrate:status": "prisma migrate status --schema prisma/schema.prisma || prisma validate --schema prisma/schema.prisma",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "db:migrate:dev": "prisma migrate dev --schema prisma/schema.prisma",
    "db:seed": "node --import tsx src/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "6.3.0"
  },
  "devDependencies": {
    "prisma": "6.3.0",
    "tsx": "4.19.2"
  }
}
